interface:
  display_name: "CASE Task Normalization"
  short_description: "Normalize tasks with Harbor as the default"
  default_prompt: "Use $case-harbor-normalization during CASE task cleaning to interpret this delivery, normalize faithfully to Harbor by default, and continue task processing through separately recorded sandbox checks."
