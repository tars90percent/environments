# Optional lark-cli configuration profile dedicated to the agent.
LARK_PROFILE=

# Uses the already-installed Codex CLI during local development.
CODEX_PATH=codex

# During the pilot, respond in direct messages only. Set true to allow group
# mentions after the bot has been tested privately.
ALLOW_GROUP_CHATS=false

# Deliberate escape hatch for a later company-wide pilot. Keep false initially.
ALLOW_ALL_USERS=false

# Keep the agent's Codex workspace separate from the procurement workspace.
# The source-controlled operating guide is copied here as AGENTS.md on startup.
AGENT_WORKSPACE=.data/workspace
AGENT_INSTRUCTIONS_FILE=AGENTS.md

# Persistent mapping from Feishu conversations to Codex threads.
AGENT_STATE=.data/state.json

# Codex tool permissions. Keep these defaults for local development. In an
# isolated cloud container, full access makes the container the boundary.
CODEX_SANDBOX_MODE=read-only
CODEX_NETWORK_ACCESS=false
CODEX_WEB_SEARCH_MODE=disabled
CODEX_APPROVAL_POLICY=never
CODEX_APPROVALS_REVIEWER=user

# Optional comma-separated Feishu open_ids allowed to use the pilot.
# Leave empty only for a tightly controlled development app.
ALLOWED_USER_IDS=

# CASE-owned canonical registry. Set all five together to enable the API and
# CASE registry tools. The portal receives only its narrow catalog, review, and
# upload tokens.
DATABASE_URL=
CASE_REGISTRY_CATALOG_TOKEN=
CASE_REGISTRY_REVIEW_TOKEN=
CASE_REGISTRY_UPLOAD_TOKEN=
CASE_REGISTRY_ADMIN_TOKEN=
PORT=3000

# Optional S3-compatible canonical artifact store.
AWS_ENDPOINT_URL=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=
AWS_DEFAULT_REGION=auto

# Remote task evaluation. Harbor and the Modal CLI are installed in the CASE
# image. `harbor run` is forced onto Modal and writes downloaded results below
# CASE_HARBOR_WORKDIR on the persistent Railway volume.
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
MODAL_ENVIRONMENT=
CASE_HARBOR_WORKDIR=/data/evaluations
CASE_HARBOR_HOME=/data/harbor-home

# Optional comma-separated evaluation-only variables to expose to Harbor.
# CASE registry, object-store, Feishu, portal, and database credentials are
# always blocked. Common model API keys are passed automatically when set.
CASE_HARBOR_ALLOWED_ENV=
